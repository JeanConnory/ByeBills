<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="ByeBills.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:ByeBills"
    xmlns:views="clr-namespace:ByeBills.Views"
    Shell.FlyoutBehavior="Flyout">

    <FlyoutItem FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent Title="Home" ContentTemplate="{DataTemplate local:MainPage}" />
        <ShellContent Title="Categorias" ContentTemplate="{DataTemplate views:CategoriaPage}" />
    </FlyoutItem>

    <!--<ShellContent
        Shell.NavBarIsVisible="true"
        Title="Monkeys"
        Route="MainPage" />-->


</Shell>
